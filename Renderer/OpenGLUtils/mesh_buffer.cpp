#include "mesh_buffer.hpp"

/*
void MeshBuffer::uploadData()
{
	auto vertices = mesh->getVertexData();
	auto face_indeces = mesh->getFaceIndeces();
	auto normals = mesh->getNormals();
	glBindBuffer(GL_ARRAY_BUFFER, vbo);
	glBufferData(GL_ARRAY_BUFFER, sizeof(decltype(vertices)::value_type) * vertices.size(), vertices.data(), GL_STATIC_DRAW);
	glBindBuffer(GL_ELEMENT_ARRAY_BUFFER, ebo);
	glBufferData(GL_ELEMENT_ARRAY_BUFFER, sizeof(decltype(face_indeces)::value_type) * face_indeces.size(), face_indeces.data(), GL_STATIC_DRAW);
	glBindBuffer(GL_ARRAY_BUFFER, vboNormals);
	glBufferData(GL_ARRAY_BUFFER, sizeof(decltype(normals)::value_type) * normals.size(), normals.data(), GL_STATIC_DRAW);
}
*/


